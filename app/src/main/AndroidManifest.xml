<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cegepst.example.lunatics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lunatics">
        <activity android:name=".views.activities.GamesByGenreActivity"></activity>
        <activity android:name=".views.activities.ActivityGenres" />
        <activity android:name=".views.activities.GamesByPlatformActivity" />
        <activity android:name=".views.activities.PlatformsActivity" />
        <activity android:name=".views.activities.GamesToComeActivity" />
        <activity
            android:name=".views.activities.TEST"
            android:label="@string/title_activity_t_e_s_t"
            android:theme="@style/Theme.Lunatics.NoActionBar" />
        <activity android:name=".views.activities.NewGamesActivity" />
        <activity android:name=".views.activities.MainActivity" />
        <activity android:name=".views.activities.SingleGameActivity" />
        <activity android:name=".views.activities.SameSeriesActivity" />
        <activity android:name=".views.activities.GameAchievementActivity" />
        <activity android:name=".views.activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>